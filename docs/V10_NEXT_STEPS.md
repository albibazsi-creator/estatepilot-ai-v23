# V10 Next Steps

1. Run `npm install` locally.
2. Run `npm run db:push && npm run db:seed`.
3. Run `npm run release:v10-check`.
4. Fix Prisma/type/build errors.
5. Replace demo current-user stub with Clerk/Auth.js.
6. Replace mock AI with OpenAI calls and AI decision logging.
7. Implement real cloud upload and image optimization.
8. Deploy staging environment and test the full demo script.
