# EstatePilot SDK examples

This folder contains lightweight partner integration examples. The TypeScript client is intentionally fetch-only so it can run in Node, Next.js route handlers, serverless functions, and simple scripts.

Production TODO:
- publish as `@estatepilot/sdk`
- add typed response DTOs generated from an OpenAPI schema
- add retry/backoff and request-id headers
- add webhook signature verification helpers
