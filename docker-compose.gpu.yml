services:
  spatial-gpu-worker:
    build:
      context: ./workers/spatial-gpu
    environment:
      SPATIAL_ARTIFACT_CDN_URL: ${SPATIAL_ARTIFACT_CDN_URL:-/mock-spatial}
      SPATIAL_WEBHOOK_SECRET: ${SPATIAL_WEBHOOK_SECRET:-dev-webhook-secret}
    # Production note: run on a GPU host and add NVIDIA runtime settings there.
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - capabilities: [gpu]
